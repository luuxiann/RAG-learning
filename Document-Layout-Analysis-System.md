# Document-Layout-Analysis-System

```
conda deactivate
conda create -n dlas python=3.9
cd Document-Layout-Analysis-System
conda activate dlas
pip install -r requirements.txt
conda install -c conda-forge tesseract          # 无sudo权限，所以用conda
```


