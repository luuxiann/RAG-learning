# DAY 2
## 完成任务



## 遇到的问题
1. 在配置好MutimodelRAG需求的所有环境后，运行发现无法连接上智谱。
查了一下，发现是未实名认证，实名认证后的创建的API才能用，实名认证前的API即便实名认证了也还是不能用。
可以运行一下这个确保加载。
    ```
    from dotenv import load_dotenv
    load_dotenv()  # 加载 .env 中的环境变量
    ```
    嘶，好像还是不可以，MDocAgent的数据集下好了，先整这个。
2. 运行MutimodelRAG的代码时，无法连上hugging face使用其CLIP模型。
    ```
    export HF_ENDPOINT=https://hf-mirror.com    # 用镜像来调用hugging face上的CLIP模型
    python MultimodalRAG.py                     # 运行
    ```
    用镜像也不能嘞，只能导入本地再挪到服务器了
3. MDocAgent的数据集直接在服务器上下不下来，一直显示连不上hugging face，用了镜像也不行。
将数据集直接git clone到本地， .json文件也没有出问题。
（上次在虚拟机用git clone获得的.json不能用，还以为这个方法不行。 ）
