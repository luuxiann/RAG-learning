# DAY 4
(不知道今天能不能把MDocAgent的代码跑完，可能看需要的模型下载速度吧？)


### 遇到问题
1. 运行python scripts/retrieve.py --config-name mmlb时程序在处理同一个文件时陷入无限循环，每次都尝试编译CUDA扩展并失败。
```
# 先安装支持CUDA的PyTorch
pip uninstall -y torch torchvision torchaudio
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# 然后安装faiss-gpu
pip uninstall -y faiss-cpu
pip install faiss-gpu-cuda12                        # 报错

conda install -c conda-forge faiss-gpu      # 使用conda安装
# 自己尝试了网上查的方法，也请师兄帮忙了，还是不可以，好像有个什么不兼容
```

